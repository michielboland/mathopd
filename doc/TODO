Things that need to be cleaned up / clarified before the 1.5 release.

When passing headers on to CGI scripts we should coalesce headers
that have the same field-name.

RFC2616, section 8.2.3 (Use of the 100 (Continue) Status)
This is not done properly.

GET Requests that include a message body confuse the server.
